# GitHub 上传前检查清单

## ✅ 必须完成的步骤

### 1. 模型文件处理
- [ ] 已将 `best_model.pt` (1.16 GB) 上传到网盘
- [ ] 已获取网盘分享链接和提取码
- [ ] 已在以下文件中填写网盘链接：
  - [ ] `README.md`（第 52 行）
  - [ ] `INSTALL.md`（第 20 行）
  - [ ] `模型训练/README.md`（第 9 行）
  - [ ] `模型训练/训练输出/.gitkeep`（第 3 行）

### 2. 文件清理
- [ ] 确认 `.gitignore` 已创建
- [ ] 确认以下大文件不会被提交：
  - [ ] `模型训练/训练输出/best_model.pt`
  - [ ] `模型训练/预训练模型/` 目录
  - [ ] `网页/legal-ai-web/node_modules/` 目录
  - [ ] `__pycache__/` 目录
  - [ ] 各种缓存和日志文件

### 3. 敏感信息检查
- [ ] 检查代码中是否有硬编码的密码、API 密钥
- [ ] 确认 `.env` 文件已在 `.gitignore` 中
- [ ] 确认 `.env.example` 已创建且不含敏感信息
- [ ] 检查是否有个人信息需要移除

### 4. 文档完善
- [ ] `README.md` 已创建并填写完整
- [ ] `INSTALL.md` 安装指南已创建
- [ ] `模型训练/README.md` 已创建
- [ ] 在 README 中添加你的联系方式（可选）
- [ ] 在 README 中添加许可证信息（可选）

### 5. 依赖文件检查
- [ ] `agent/requirements.txt` 存在且完整
- [ ] `网页/legal-ai-web/package.json` 存在且完整
- [ ] 确认依赖版本号正确

## 📝 需要手动填写的内容

在上传前，请在以下文件中填写相应信息：

### README.md
- 第 52 行：`[在此处填写你的网盘链接]`
- 第 53 行：`[在此处填写提取码]`
- 第 168 行：`[在此处添加许可证信息]`
- 第 180 行：`[GitHub Issues]` - 替换为实际的 Issues 链接
- 第 181 行：`[在此处添加邮箱]`

### INSTALL.md
- 第 8 行：`[你的 GitHub 仓库地址]`
- 第 9 行：`[项目目录名]`
- 第 20 行：`[在此处填写你的网盘链接]`
- 第 21 行：`[在此处填写提取码]`

### 模型训练/README.md
- 第 9 行：`[在此处填写你的网盘链接]`
- 第 10 行：`[在此处填写提取码]`

### 模型训练/训练输出/.gitkeep
- 第 3 行：`[在此处填写你的网盘链接]`

## 🚀 上传步骤

### 1. 初始化 Git 仓库（如果还没有）

```bash
git init
git add .
git commit -m "Initial commit: 法律 AI 智能分析系统"
```

### 2. 连接到 GitHub 远程仓库

```bash
# 在 GitHub 上创建新仓库后
git remote add origin [你的仓库地址]
git branch -M main
git push -u origin main
```

### 3. 验证上传

- [ ] 访问 GitHub 仓库页面
- [ ] 确认文件已正确上传
- [ ] 确认大文件未被上传
- [ ] 测试 README 显示是否正常
- [ ] 检查网盘链接是否可访问

## ⚠️ 注意事项

### 文件大小警告
如果 Git 提示文件过大，说明 `.gitignore` 可能未生效：

```bash
# 清除 Git 缓存
git rm -r --cached .
git add .
git commit -m "Update .gitignore"
```

### 检查将要提交的文件
```bash
# 查看将要提交的文件
git status

# 查看文件大小
git ls-files | xargs ls -lh
```

### 如果不小心提交了大文件
```bash
# 从 Git 历史中移除大文件
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch 模型训练/训练输出/best_model.pt" \
  --prune-empty --tag-name-filter cat -- --all

# 强制推送
git push origin --force --all
```

## 📊 预期仓库大小

正确配置后，仓库大小应该在：
- 代码文件：< 50 MB
- 不包含 node_modules
- 不包含模型文件
- 不包含缓存文件

## ✨ 可选优化

- [ ] 添加 GitHub Actions 自动化测试
- [ ] 添加 LICENSE 文件
- [ ] 添加 CONTRIBUTING.md 贡献指南
- [ ] 添加项目徽章（stars, license, etc.）
- [ ] 创建 GitHub Pages 文档站点
- [ ] 添加 Issue 模板
- [ ] 添加 Pull Request 模板

## 🎉 完成

全部检查完成后，你的项目就可以成功上传到 GitHub 了！

记得在网盘中上传 `best_model.pt` 文件，并在 README 中更新链接。
